<p style="color: green"><%= notice %></p>

    <%= @client.id %> <br>
    <%= @client.name %> <br>
    <%= @client.age %>  <br>
    <%= @client.movies.last %> <br>

<div class="container">
  <%= link_to "Edit this client", edit_client_path(@client) %> |
  <%= link_to "Back to clients", clients_path %>

  <%= button_to "Destroy this client", @client, method: :delete, data: { confirm: "seguro que deseas eliminar el articulo?"} %>
</div>
